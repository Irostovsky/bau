<% semantic_form_for([@gallery, @photo], :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
	<% f.inputs :title => 'Add Photo' do %>
		<%= f.input :avatar, :as => :file  %>
		<%= f.buttons %>
	<% end -%>
	
<% end %>

<%= link_to 'Back', gallery_path(@gallery) %>